---
const { astroCarousel } = import('@astro-carousel');
const { images } = Astro.props;
---

<carousel>
  <carousel>
    {images.map((image, index) => (
      <img src={astroCarousel(`public/assets/images/carousel/${image}`)} alt={`Image ${index + 1}`} />
    ))}
  </carousel>
</carousel>
